name: totp-extension
type: make
buildCommand: echo Installing TOTP extension...
source:
  type: git
  url: https://github.com/dkosmari/gnome-shell-extension-totp
  branch: master
  commit: latest
modules:
  - name: build-deps
    type: apt
    source:
      packages:
      - jq
