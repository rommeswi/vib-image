name: gnome-extensions-installer
type: shell
commands:
- bash /gnome-extensions/download-extensions.sh
- rm -rf /gnome-extensions
modules:
- name: extension-requirements
  type: apt
  source:
    packages:
    - yq
    - jq
    - unzip
